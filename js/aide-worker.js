// $('.subscribe-link').show();
// 
// var port = chrome.runtime.connect();
// window.addEventListener('message',function(event) {
// 	if (event.source != window) return;
// 	if ('ra-action' == event.data.type && 'show-pro-account' == event.data.text)
// 		chrome.storage.local.set({'show-pro-account':Math.floor(Math.random() * 65535)});
// },false);
